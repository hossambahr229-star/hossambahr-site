/* Public runtime settings only. Never place credentials, tokens, or private keys here. */
window.HB_RUNTIME = Object.freeze({
  environment: 'development',
  apiBaseUrl: '',
  authEnabled: false,
  serviceRequestsEnabled: false,
  privateDocumentsEnabled: false,
  hostedPaymentsEnabled: false,
  liveTrackingEnabled: false
});
